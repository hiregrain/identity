// Code generated by db/typegen.mjs from the live spine schema. DO NOT EDIT.

// Row types for the spine plane only, generated per database with no
// shared namespace. The unique-symbol brand makes the types nominal:
// a payload row cannot satisfy a spine-typed parameter even
// when the row shapes coincide.
export declare const spinePlane: unique symbol;

/** One row of spine.cross_plane_outbox. */
export interface CrossPlaneOutboxRow {
  readonly [spinePlane]: "spine";
  entry_id: string;
  target_plane: string;
  instruction: Uint8Array;
  created_at: string;
}

/** One row of spine.cross_plane_outbox_attempts. */
export interface CrossPlaneOutboxAttemptsRow {
  readonly [spinePlane]: "spine";
  entry_id: string;
  attempt: number;
  state: string;
  detail: string;
  noted_at: string;
}

/** One row of spine.deletion_journal. */
export interface DeletionJournalRow {
  readonly [spinePlane]: "spine";
  person_id: string;
  requester_class: string;
  recorded_at: string;
}

/** One row of spine.key_event. */
export interface KeyEventRow {
  readonly [spinePlane]: "spine";
  party_id: string;
  key_id: string;
  event: string;
  effective_at: string;
  ledger_ts: string;
}

/** One row of spine.person. */
export interface PersonRow {
  readonly [spinePlane]: "spine";
  person_id: string;
  created_at: string;
}

/** One row of spine.person_lifecycle_transition. */
export interface PersonLifecycleTransitionRow {
  readonly [spinePlane]: "spine";
  person_id: string;
  state: string;
  recorded_at: string;
}

/** One row of spine.schema_migrations. */
export interface SchemaMigrationsRow {
  readonly [spinePlane]: "spine";
  number: number;
  name: string;
  applied_at: string;
}

/** One row of spine.stream_heads. */
export interface StreamHeadsRow {
  readonly [spinePlane]: "spine";
  stream_type: string;
  stream_key: string;
  link_position: number;
  head_hash: Uint8Array;
  updated_at: string;
}

/** One row of spine.stream_link. */
export interface StreamLinkRow {
  readonly [spinePlane]: "spine";
  stream_type: string;
  stream_key: string;
  link_position: number;
  record_id: string;
  prev_hash: Uint8Array;
  link_hash: Uint8Array;
  recorded_at: string;
}
